/**
 * RC6 core — retired in DICTION DASH Phase 7.
 *
 * This design pass gave DESCENT its authored Big-Air beats and the physical
 * hunt pursuit. The flat track has no beats to author and the pursuit is a
 * pure speed differential (sim/beast.js), so the pass applies nothing.
 * The entry point survives because the sim's import surface is stable.
 */

export function applyRC6Core() {}

export default applyRC6Core;
